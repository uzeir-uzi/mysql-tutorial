-- Order by, sorts in acending or decending order 

# select *
# from employee_demographics
#order by first_name; # order our rows acending order a-z. we can use asc or desc for acedning or decending

select *
from employee_demographics
order by gender, age
